---
interface Props {
  title: string;
  href?: string;
  linkText?: string;
}

const { title, href, linkText = 'View all →' } = Astro.props;
---

<div class="section-header">
  <h2 class="section-title">{title}</h2>
  {href && (
    <a href={href} class="btn btn-ghost text-xs">
      {linkText}
    </a>
  )}
</div>